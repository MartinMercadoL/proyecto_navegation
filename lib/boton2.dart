import 'package:flutter/material.dart';

class boton2 extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Segundo apartado'),
        backgroundColor: Colors.pink,
      ),
    );
  }
}
